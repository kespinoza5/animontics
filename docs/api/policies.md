# policies — API Reference

Policy plugin packages. The base class, `PolicyRuntime`, and registry live in
`core.policy`; the types below are the concrete plugins in `policies/`.

## policy (core)

::: core.policy.PolicyBase
::: core.policy.PolicyRuntime
::: core.policy.create_policy

## curve

::: policies.curve.policy.CurvePolicy
