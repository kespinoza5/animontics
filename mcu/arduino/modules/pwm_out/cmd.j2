if (cmd == 1 && n >= 2) {   // CMD_SET_DUTY: args = [channel, duty]
  const int16_t ch = args[0];
  if (ch >= {{ cmd_offset }} && ch < {{ cmd_offset + npins }}) {{ inst }}.set_duty((uint8_t)(ch - {{ cmd_offset }}), (uint8_t)args[1]);
}