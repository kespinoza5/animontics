module: transport_serial
platforms: [arduino]
role: transport
description: Frame the sample vector onto the USB/UART uplink (protocol v1).
claims:
  pins: uart                # the hardware UART / USB-CDC link (D0/D1)
sources: [transport_serial.h, transport_serial.cpp]
