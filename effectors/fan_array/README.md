# fan_array — fan control effector (placeholder)

Fan-aware effector built on the `pwm` drive: named fans, per-fan `min_duty`, and
(later) RPM surfaced from a paired `fan_tach` sensor. Built in Phase F of the
restructure plan — see [effectors/](../README.md) and `effectors/pwm`.
